---
- name: Join Linux Server to Windows AD Domain
  hosts: all
  become: yes

  vars:
    ad_domain: "corp.jitb.net"
    # computer_ou: "OU=LinuxServers,DC=corp,DC=jitb,DC=net"  # Uncomment and set if needed

    # Restrict login to this AD security group (Ubuntu 20.04 + SSSD)
    ad_access_filter: "(memberOf=CN=LNX-LGN-ALLOW-TST-ZABBIX,OU=Manual,OU=SecurityGroups,OU=_JIB,DC=corp,DC=jitb,DC=net)"

  vars_prompt:
    - name: "ad_admin_user"
      prompt: "Enter the domain admin username"
      private: no
    - name: "ad_admin_password"
      prompt: "Enter the domain admin password"
      private: yes  

  tasks:
    - name: Ensure required packages are installed (RHEL)
      ansible.builtin.package:
        name:
          - realmd
          - sssd
          - sssd-ad
          - oddjob
          - oddjob-mkhomedir
          - adcli
          - samba-common
          - samba-common-tools
          - krb5-libs
          - krb5-workstation
          - openldap-clients
          - policycoreutils-python
        state: present
      when: ansible_os_family == "RedHat"

    - name: Ensure required packages are installed (Debian/Ubuntu)
      ansible.builtin.package:
        name:
          - realmd
          - sssd
          - sssd-tools
          - adcli
          - libnss-sss 
          - libpam-sss 
          - samba-common
          - samba
          - samba-common-bin
          - krb5-user
          - packagekit
          - oddjob
          - oddjob-mkhomedir
          - ldap-utils
          - policycoreutils-python-utils
        state: present
      when: ansible_os_family == "Debian"

    - name: Ensure the realm is discoverable
      ansible.builtin.command: realm discover {{ ad_domain }}
      register: realm_discover
      changed_when: false
      failed_when: realm_discover.rc != 0

    - name: Join the machine to the AD domain
      ansible.builtin.command: >
        realm join {{ ad_domain }}
        --user={{ ad_admin_user }}
        --verbose
      args:
        stdin: "{{ ad_admin_password }}"
      register: join_result
      changed_when: "'already joined' not in join_result.stdout"
      failed_when:
        - join_result.rc != 0
        - "'already joined' not in join_result.stdout"

    - name: Allow domain users to log in (initially)
      ansible.builtin.command: realm permit --all
      register: permit_result
      changed_when: permit_result.rc == 0

    # ==================== SSSD CONFIGURATION ====================

    - name: Ensure home directories are created automatically
      ansible.builtin.lineinfile:
        path: /etc/sssd/sssd.conf
        regexp: '^#?override_homedir ='
        line: 'override_homedir = /home/%d/%u'
        insertafter: '[domain/{{ ad_domain }}]'
        backup: yes
      notify: Restart services sssd and sshd

    - name: Configure SSSD with a Fallback Home Directory
      ansible.builtin.lineinfile:
        path: /etc/sssd/sssd.conf
        regexp: '^#?fallback_homedir ='
        line: 'fallback_homedir = /home/%u'
        insertafter: '[domain/{{ ad_domain }}]'
        backup: yes
      notify: Restart services sssd and sshd

    - name: Ensure user login without @domain suffix
      ansible.builtin.lineinfile:
        path: /etc/sssd/sssd.conf
        regexp: '^#?full_name_format ='
        line: 'full_name_format = %1$s'
        insertafter: '[domain/{{ ad_domain }}]'
        backup: yes
      notify: Restart services sssd and sshd

    - name: Add default_domain_suffix
      lineinfile:
        path: "/etc/sssd/sssd.conf"
        line: "default_domain_suffix = {{ ad_domain }}"
        insertafter: "services = nss, pam"    
        backup: yes
      notify: Restart services sssd and sshd

    # === NEW TASK: Restrict login to specific AD security group ===
    - name: Restrict AD login to specific security group (ad_access_filter)
      ansible.builtin.lineinfile:
        path: /etc/sssd/sssd.conf
        regexp: '^#?ad_access_filter ='
        line: "ad_access_filter = {{ ad_access_filter }}"
        insertafter: '[domain/{{ ad_domain }}]'
        backup: yes
      notify: Restart services sssd and sshd
      when: ansible_os_family == "Debian"   # As requested for Ubuntu 20.04

    # ==================== SSH CONFIG ====================

    - name: Change GSSAPIAuthentication value
      lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^GSSAPIAuthentication\s+yes'
        line: 'GSSAPIAuthentication no'
        backup: yes
      notify: Restart services sssd and sshd

    # ==================== PAM CONFIG ====================

    - name: Ensure pam_mkhomedir is configured for Ubuntu/Debian
      lineinfile:
        path: "/etc/pam.d/common-session"
        regexp: '^session\s+required\s+pam_mkhomedir\.so'
        line: 'session required pam_mkhomedir.so skel=/etc/skel/ umask=0077'
        state: present
      when: ansible_os_family == "Debian"

    - name: Ensure pam_mkhomedir is configured for RHEL
      lineinfile:
        path: "/etc/pam.d/system-auth"
        regexp: '^session\s+required\s+pam_mkhomedir\.so'
        line: 'session required pam_mkhomedir.so skel=/etc/skel/ umask=0077'
        state: present
      when: ansible_os_family == "RedHat"

  handlers:
    - name: Restart services sssd and sshd
      ansible.builtin.service:
        name: "{{ item }}"
        state: restarted
      with_items:
        - sssd
        - sshd
